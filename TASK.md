# TASK — creditcard

이 파일은 `pds2225/creditcard`의 AI 작업지시 단일 기준이다.

## CURRENT TASK
현재 등록된 과업 없음.

## EXECUTION CONTRACT
- 작업 전 저장소가 `pds2225/creditcard`인지 확인한다.
- 원격 기본 브랜치의 `TASK.md`를 처음부터 끝까지 읽고, 이 파일에 적힌 과업만 수행한다.
- 다른 저장소의 TASK/TASKS/NEXT_TASK 내용을 추론하거나 가져오지 않는다.
- CURRENT TASK가 비어 있으면 `NO_ACTIVE_TASK`만 보고하고 멈춘다.
- 과업이 여러 개면 독립 가능한 작업은 별도 브랜치로 병렬 수행한다.
- 기본 브랜치에서 직접 개발하지 않는다.
- TASK.md에 별도 지시가 없으면 구현·테스트·commit·push까지만 하고 기본 브랜치는 병합하지 않는다.
